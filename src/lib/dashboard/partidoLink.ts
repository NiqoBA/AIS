/** Estilo de enlace genérico usado donde antes apuntaba a partidos (ahora inicio del radar). */
export const PARTIDO_LINK_CLASS =
  "text-blue-600 underline underline-offset-[3px] decoration-solid hover:text-blue-800";

export function hrefPartidoDemo(_partidoId: string): string {
  return "/dashboard";
}
